# Development Environment Configuration
NODE_ENV=development

# Server Configuration
PORT=3000
CORS_ORIGIN=http://localhost:5173

# JWT Configuration (for development only)
JWT_SECRET=development-secret-key-at-least-32-characters-long-for-dev-only

# CouchDB Configuration
COUCHDB_URL=http://admin:password@localhost:5984
COUCHDB_DB_NAME=todos-dev

# CouchDB Authentication
COUCHDB_ADMIN_USERNAME=admin
COUCHDB_ADMIN_PASSWORD=password

# Client Configuration (VITE_ prefix required for client-side access)
VITE_API_URL=http://localhost:3000/api